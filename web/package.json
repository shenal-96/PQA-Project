{
  "name": "pqa-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "PQA web frontend — shared by the Windows desktop shell (PyWebview) and, later, the iPad PWA.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.19.0",
    "svelte-check": "^4.1.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "echarts": "^5.6.0"
  }
}
